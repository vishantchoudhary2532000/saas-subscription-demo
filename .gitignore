# Ignore everything by default
*

# But don't ignore these folders and their contents
!frontend/
!backend/
!.gitignore
!README.md

# Allow all files in frontend and backend (their own .gitignore will handle what to ignore)
!frontend/**
!backend/**

# Ignore other root level files and folders
check-node-version.js
documents/
*.md
!README.md

# Environment files (root level only)
.env
.env.local
.env*.local

# IDE (root level only)
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs (root level only)
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp
